<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-50">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ .Title }} - Notes</title>
    <link rel="stylesheet" href="/static/css/main.css">
    <script src="https://unpkg.com/htmx.org@2.0.2"></script>
    <script src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
</head>
<body class="h-full flex flex-col text-gray-800 antialiased">
    <header class="py-4 px-6 bg-white shadow-sm">
        <h1 class="text-xl font-light">Notes</h1>
    </header>
    <main class="flex-grow container mx-auto px-4 py-8">
        {{ template "content" . }}
    </main>
    <footer class="py-4 px-6 text-center text-sm text-gray-500">
        <p>Simple notes, shared everywhere.</p>
    </footer>
    {{ template "scripts" . }}
</body>
</html>